import { ResetPasswordPipe } from './reset-password.pipe';

describe('ResetPasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new ResetPasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
