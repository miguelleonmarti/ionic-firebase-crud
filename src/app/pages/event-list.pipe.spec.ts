import { EventListPipe } from './event-list.pipe';

describe('EventListPipe', () => {
  it('create an instance', () => {
    const pipe = new EventListPipe();
    expect(pipe).toBeTruthy();
  });
});
