import { EventDetailPipe } from './event-detail.pipe';

describe('EventDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new EventDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
